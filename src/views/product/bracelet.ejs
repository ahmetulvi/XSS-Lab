<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - Future Tech</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <style>
      /* Override main tag max-width for this page only */
      main {
        max-width: none !important;
        margin: 0 !important;
        padding: 0 !important;
        background-color: transparent !important;
        box-shadow: none !important;
        border-radius: 0 !important;
      }

      /* Modern Product Section */
      .product-hero {
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        padding: 20px 0;
        text-align: center;
      }

      .product-hero h1 {
        font-size: 3.5rem;
        font-weight: 700;
        color: #2c3e50;
        margin-bottom: 15px;
        letter-spacing: -1px;
      }

      .product-hero p {
        font-size: 1.3rem;
        color: #7f8c8d;
        margin-bottom: 20px;
      }

      .product-price-hero {
        font-size: 1.5rem;
        color: #3498db;
        font-weight: 600;
      }

      .product-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 80px 40px;
      }

      .product-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
      }

      .product-image-section {
        background: white;
        border-radius: 20px;
        padding: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      .product-image-section img {
        max-width: 100%;
        height: auto;
        border-radius: 15px;
      }

      .product-details {
        padding: 40px 0;
      }

      .product-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: #2c3e50;
        margin-bottom: 20px;
        line-height: 1.2;
      }

      .product-description {
        font-size: 1.2rem;
        color: #7f8c8d;
        line-height: 1.6;
        margin-bottom: 30px;
      }

      .price-section {
        background: #f8f9fa;
        padding: 25px;
        border-radius: 15px;
        margin-bottom: 30px;
      }

      .price {
        font-size: 2.5rem;
        font-weight: 700;
        color: #2c3e50;
        margin-bottom: 5px;
      }

      .price-note {
        color: #27ae60;
        font-weight: 500;
      }

      .features-section {
        margin-bottom: 40px;
      }

      .features-title {
        font-size: 1.8rem;
        font-weight: 600;
        color: #2c3e50;
        margin-bottom: 20px;
      }

      .features-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 15px;
      }

      .feature-item {
        display: flex;
        align-items: center;
        padding: 15px;
        background: white;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
        transition: transform 0.3s ease;
      }

      .feature-item:hover {
        transform: translateY(-2px);
      }

      .feature-item i {
        color: #3498db;
        margin-right: 12px;
        font-size: 1.2rem;
        width: 20px;
      }

      .feature-item span {
        color: #2c3e50;
        font-weight: 500;
      }

      .buy-button {
        background: linear-gradient(135deg, #3498db, #2980b9);
        color: white;
        border: none;
        padding: 18px 40px;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 50px;
        cursor: pointer;
        transition: all 0.3s ease;
        text-transform: uppercase;
        letter-spacing: 1px;
        box-shadow: 0 10px 20px rgba(52, 152, 219, 0.3);
      }

      .buy-button:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 30px rgba(52, 152, 219, 0.4);
      }

      /* Comments Section */
      .comments-section {
        background: #f8f9fa;
        margin-top: 80px;
        padding: 60px 40px;
      }

      .comments-container {
        max-width: 1200px;
        margin: 0 auto;
      }

      .section-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: #2c3e50;
        text-align: center;
        margin-bottom: 50px;
      }

      .comments-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
      }

      .comments-list {
        background: white;
        border-radius: 20px;
        padding: 40px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      }

      .comment-form {
        background: white;
        border-radius: 20px;
        padding: 40px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      }

      .comment {
        background: #f8f9fa;
        border-radius: 15px;
        padding: 20px;
        margin-bottom: 20px;
        border-left: 4px solid #3498db;
      }

      .comment-author {
        font-weight: 600;
        color: #2c3e50;
        margin-bottom: 8px;
      }

      .comment-date {
        font-size: 0.9rem;
        color: #7f8c8d;
        margin-left: 10px;
      }

      .comment-text {
        color: #2c3e50;
        line-height: 1.6;
        margin-top: 10px;
      }

      .form-group {
        margin-bottom: 25px;
      }

      .form-group label {
        display: block;
        font-weight: 600;
        color: #2c3e50;
        margin-bottom: 8px;
        font-size: 1.1rem;
      }

      .form-group input,
      .form-group textarea {
        width: 100%;
        padding: 15px;
        border: 2px solid #e0e0e0;
        border-radius: 10px;
        font-size: 1rem;
        transition: border-color 0.3s ease;
        background: white;
      }

      .form-group input:focus,
      .form-group textarea:focus {
        outline: none;
        border-color: #3498db;
      }

      .submit-button {
        background: linear-gradient(135deg, #2c3e50, #34495e);
        color: white;
        border: none;
        padding: 15px 30px;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 50px;
        cursor: pointer;
        transition: all 0.3s ease;
        width: 100%;
      }

      .submit-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(44, 62, 80, 0.3);
      }

      .back-link {
        display: block;
        text-align: center;
        margin: 40px auto;
        color: #3498db;
        text-decoration: none;
        font-size: 1.1rem;
        font-weight: 500;
        transition: color 0.3s ease;
      }

      .back-link:hover {
        color: #2980b9;
      }

      .back-link i {
        margin-right: 8px;
      }

      /* Responsive */
      @media (max-width: 992px) {
        .product-grid,
        .comments-grid {
          grid-template-columns: 1fr;
          gap: 40px;
        }

        .features-grid {
          grid-template-columns: 1fr;
        }

        .product-hero h1 {
          font-size: 2.5rem;
        }

        .product-container,
        .comments-section {
          padding: 40px 20px;
        }
      }

      @media (max-width: 768px) {
        .product-hero h1 {
          font-size: 2rem;
        }

        .product-title {
          font-size: 2rem;
        }

        .section-title {
          font-size: 2rem;
        }
      }
    </style>
  </head>
  <body>
    <main>
      <!-- Success/Error Messages -->
      <script>
        // Check for URL parameters to show messages
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get("status") === "cleared") {
          const alertDiv = document.createElement("div");
          alertDiv.style.cssText = `
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: white;
            padding: 15px 20px;
            text-align: center;
            margin: 20px auto;
            max-width: 1200px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(39, 174, 96, 0.3);
            font-weight: 500;
          `;
          alertDiv.innerHTML =
            '<i class="fas fa-check-circle"></i> All reviews cleared successfully! Original sample reviews restored.';
          document
            .querySelector("main")
            .insertBefore(alertDiv, document.querySelector(".product-hero"));

          // Remove the parameter from URL after 3 seconds
          setTimeout(() => {
            window.history.replaceState({}, document.title, "/product");
          }, 3000);
        } else if (urlParams.get("status") === "clear_error") {
          const alertDiv = document.createElement("div");
          alertDiv.style.cssText = `
            background: linear-gradient(135deg, #e74c3c, #c0392b);
            color: white;
            padding: 15px 20px;
            text-align: center;
            margin: 20px auto;
            max-width: 1200px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(231, 76, 60, 0.3);
            font-weight: 500;
          `;
          alertDiv.innerHTML =
            '<i class="fas fa-exclamation-triangle"></i> Error clearing reviews. Please try again.';
          document
            .querySelector("main")
            .insertBefore(alertDiv, document.querySelector(".product-hero"));

          // Remove the parameter from URL after 5 seconds
          setTimeout(() => {
            window.history.replaceState({}, document.title, "/product");
          }, 5000);
        }
      </script>

      <!-- Product Hero -->
      <section class="product-hero">
        <h1><%= title %></h1>
        <p>Harness the power of your movement. Stay charged, sustainably.</p>
        <div class="product-price-hero">Starting at $199.99</div>
      </section>

      <!-- Product Section -->
      <div class="product-container">
        <div class="product-grid">
          <div class="product-image-section">
            <img
              src="/assets/PiezoelectricBracelet.png"
              alt="Piezoelectric Energy Bracelet"
            />
          </div>

          <div class="product-details">
            <h2 class="product-title">Revolutionary Energy Technology</h2>
            <p class="product-description">
              Introducing the revolutionary Piezoelectric Energy Bracelet – a
              cutting-edge wearable that converts your kinetic energy from daily
              movements into electrical power. Never worry about a dead
              smartwatch or phone again.
            </p>

            <div class="price-section">
              <div class="price">$199.99</div>
              <div class="price-note">✓ Free shipping and returns</div>
            </div>

            <div class="features-section">
              <h3 class="features-title">Key Features</h3>
              <div class="features-grid">
                <div class="feature-item">
                  <i class="fas fa-bolt"></i>
                  <span>Piezoelectric Generators</span>
                </div>
                <div class="feature-item">
                  <i class="fas fa-sync-alt"></i>
                  <span>Energy Conversion Tech</span>
                </div>
                <div class="feature-item">
                  <i class="fas fa-usb"></i>
                  <span>USB-C Charging Port</span>
                </div>
                <div class="feature-item">
                  <i class="fas fa-shield-alt"></i>
                  <span>Water-Resistant IP67</span>
                </div>
                <div class="feature-item">
                  <i class="fas fa-chart-bar"></i>
                  <span>Energy Display</span>
                </div>
                <div class="feature-item">
                  <i class="fas fa-leaf"></i>
                  <span>Eco-Friendly Materials</span>
                </div>
              </div>
            </div>

            <button
              class="buy-button"
              onclick="alert('Added to cart! (Demo purpose)')"
            >
              Pre-Order Now
            </button>
          </div>
        </div>
      </div>

      <!-- Comments Section -->
      <section class="comments-section">
        <div class="comments-container">
          <h2 class="section-title">Customer Reviews & Feedback</h2>

          <div class="comments-grid">
            <div class="comments-list">
              <h3 style="margin-bottom: 30px; color: #2c3e50">
                Recent Reviews
              </h3>

              <% if (comments && comments.length > 0) { %> <%
              comments.forEach(function(comment) { %>
              <div class="comment">
                <div class="comment-author">
                  <%= comment.name %>
                  <span class="comment-date"><%= comment.date %></span>
                </div>
                <div class="comment-text">
                  <%- comment.comment %>
                  <!-- XSS Vulnerability: Unescaped comment -->
                </div>
              </div>
              <% }); %> <% } else { %>
              <p style="color: #7f8c8d; font-style: italic">
                No reviews yet. Be the first to share your experience!
              </p>
              <% } %>
              <!-- Clear Comments Button -->
              <form
                method="POST"
                action="/product/clear-comments"
                style="display: inline-block; margin-bottom: 20px"
                onsubmit="return confirm('Are you sure you want to clear all reviews and reset to original samples?');"
              >
                <button
                  type="submit"
                  style="
                    background: linear-gradient(135deg, #e74c3c, #c0392b);
                    color: white;
                    border: none;
                    padding: 10px 20px;
                    border-radius: 20px;
                    font-size: 0.9rem;
                    font-weight: 500;
                    cursor: pointer;
                    transition: all 0.3s ease;
                    box-shadow: 0 5px 15px rgba(231, 76, 60, 0.3);
                  "
                  onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 8px 20px rgba(231, 76, 60, 0.4)'"
                  onmouseout="this.style.transform='translateY(0px)'; this.style.boxShadow='0 5px 15px rgba(231, 76, 60, 0.3)'"
                >
                  <i class="fas fa-trash-alt"></i> Clear All Reviews
                </button>
              </form>
            </div>

            <div class="comment-form">
              <h3 style="margin-bottom: 30px; color: #2c3e50">
                Write a Review
              </h3>
              <form action="/product/comment" method="POST">
                <div class="form-group">
                  <label for="name">Your Name</label>
                  <input type="text" id="name" name="name" required />
                </div>

                <div class="form-group">
                  <label for="comment">Your Review</label>
                  <textarea
                    id="comment"
                    name="comment"
                    rows="4"
                    placeholder="Share your thoughts about the Piezoelectric Bracelet..."
                    required
                  ></textarea>
                </div>

                <button type="submit" class="submit-button">
                  Submit Review
                </button>
              </form>
            </div>
          </div>

          <a href="/" class="back-link">
            <i class="fas fa-arrow-left"></i>
            Back to XSS Lab Home
          </a>
        </div>
      </section>
    </main>
  </body>
</html>
